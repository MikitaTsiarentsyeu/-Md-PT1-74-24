# Write a program that takes a string as input and returns the string reversed.
def reversed_string(user_string):
    print(user_string[::-1])

reversed_string(user_string=input("Enter your data: "))
